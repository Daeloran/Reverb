//! Point d'entrée du binaire `reverbd`.
//!
//! Vide tant que l'issue #17 n'est pas implémentée : cette session n'écrit que la spécification
//! exécutable (`tests/spec_cadence.rs`), et un `main` qui ferait quoi que ce soit serait de
//! l'implémentation écrite avant ses propres tests.

fn main() {}
